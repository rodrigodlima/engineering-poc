# Methods to reverse string

There are many ways to reverse a string in Python. But, the best way is using the slice method, this method is the best in terms of performance and simplicity.

The slice method is showed in [DPK01_impl_1.py](DPK01_impl_1.py) file